# 🌐 Веб-интерфейс ARK v2.8

## 🚀 Быстрый старт

1. **Запуск сервера:**
   ```bash
   cd /home/a0/Desktop/ark
   source venv/bin/activate
   python3 web/chat_server.py
   ```

2. **Открытие интерфейса:**
   Откройте браузер и перейдите по адресу: `http://localhost:8000`

## 🎛️ Основные функции

### 💬 Чат с агентом
- **Ввод сообщений:** Используйте текстовое поле внизу для отправки сообщений
- **WebSocket соединение:** Автоматическое подключение для real-time общения
- **Reasoning Chain:** Просмотр логики рассуждений агента

### 🧠 Управление состоянием
- **Сознание:** normal, excited, focused, stressed, evolving, reflecting
- **Эмоции:** calm, excited, learning, concerned, frustrated, creative, curious, satisfied

### 📊 Мониторинг системы
- **Статус системы:** CPU, RAM, температура, диск I/O
- **Когнитивное состояние:** внимание, нагрузка, уверенность
- **RGB подсветка:** визуальная обратная связь

### 🏗️ Архитектор кода
- **Анализ кода:** автоматический анализ структуры проекта
- **Оптимизация:** предложения по улучшению кода

### ⚡ Быстрые действия
- **Авто-отчет:** получение текущего состояния системы
- **Тест RGB:** проверка работы подсветки
- **Очистить чат:** сброс истории сообщений
- **Экспорт чата:** сохранение истории в JSON

## 🔧 API эндпоинты

### GET /api/status
Получение текущего статуса системы
```json
{
  "status": "active",
  "consciousness_state": "excited",
  "emotion_state": "excited",
  "rgb_status": {...},
  "physical_metrics": {...}
}
```

### GET /api/chat_history
История сообщений чата
```json
{
  "messages": [...],
  "total": 10
}
```

### POST /api/change_state
Изменение состояния агента
```bash
curl -X POST "http://localhost:8000/api/change_state?consciousness_state=excited&emotion_state=excited"
```

### GET /api/reasoning_chain
Получение цепочки рассуждений
```json
{
  "reasoning_chain": [
    {"step": 1, "type": "analysis", "content": "..."},
    {"step": 2, "type": "emotional_processing", "content": "..."}
  ]
}
```

### GET /api/meta_thoughts
Мета-мысли и когнитивное состояние
```json
{
  "status": "success",
  "meta_thoughts_summary": "...",
  "cognitive_state": {...},
  "system_metrics": {...}
}
```

### POST /api/meta_thoughts/start
Запуск мониторинга мета-мыслей

### POST /api/meta_thoughts/stop
Остановка мониторинга мета-мыслей

### GET /api/architect/status
Статус архитектора кода

### POST /api/architect/analyze
Анализ кода проекта

## 🎨 RGB Подсветка

Система автоматически управляет RGB подсветкой в зависимости от состояния:

- **🔵 Синий:** Нормальное состояние (calm)
- **🟡 Желтый:** Возбужденное состояние (excited)
- **🟢 Зеленый:** Обучение (learning)
- **🟠 Оранжевый:** Стресс/беспокойство (stressed/concerned)
- **🟣 Фиолетовый:** Творчество/эволюция (creative/evolving)
- **🔴 Красный:** Ошибка/критическое состояние (error)

## 🔌 WebSocket API

### Подключение
```javascript
const ws = new WebSocket('ws://localhost:8000/ws');
```

### Отправка сообщения
```javascript
ws.send(JSON.stringify({
  type: 'user_message',
  message: 'Привет, ARK!'
}));
```

### Получение ответов
```javascript
ws.onmessage = function(event) {
  const data = JSON.parse(event.data);
  if (data.type === 'agent_response') {
    console.log('Ответ агента:', data.text);
    console.log('Reasoning chain:', data.reasoning_chain);
  }
};
```

## 🧪 Тестирование

Запустите тесты для проверки всех функций:
```bash
python3 test_interface.py
```

## 🐛 Устранение неполадок

### Проблема: Сервер не запускается
**Решение:** Проверьте, что порт 8000 свободен
```bash
lsof -i :8000
kill -9 <PID>  # если порт занят
```

### Проблема: WebSocket не подключается
**Решение:** Убедитесь, что сервер запущен и доступен
```bash
curl http://localhost:8000/api/status
```

### Проблема: RGB не работает
**Решение:** Проверьте права доступа к системным LED
```bash
sudo chmod 666 /sys/class/leds/*/brightness
```

## 📈 Мониторинг производительности

Система автоматически отслеживает:
- Использование CPU и RAM
- Температуру системы
- Активность диска и сети
- Количество процессов

## 🔒 Безопасность

- Все API эндпоинты доступны только локально
- WebSocket соединения ограничены localhost
- Нет внешних зависимостей для работы

## 🎯 Примеры использования

### 1. Изменение состояния через веб-интерфейс
1. Откройте `http://localhost:8000`
2. Нажмите кнопку "Возбужденное" в разделе "Управление состоянием"
3. Наблюдайте изменение RGB подсветки на желтый цвет

### 2. Общение с агентом
1. Введите сообщение в поле ввода
2. Нажмите Enter или кнопку "Отправить"
3. Просмотрите reasoning chain в ответе

### 3. Мониторинг системы
1. Нажмите "Обновить статус"
2. Просмотрите метрики в разделе "Системные метрики"
3. Проверьте когнитивное состояние

### 4. Экспорт данных
1. Нажмите "Экспорт чата" для сохранения истории
2. Файл будет автоматически скачан в формате JSON

## 🚀 Дальнейшее развитие

Планируемые улучшения:
- [ ] Графики в реальном времени
- [ ] Настройка пользовательских состояний
- [ ] Интеграция с внешними API
- [ ] Расширенная аналитика кода
- [ ] Мобильная версия интерфейса

---

**ARK v2.8** - Воплощенный цифровой организм с веб-интерфейсом для взаимодействия 🧠✨ 